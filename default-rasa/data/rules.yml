version: "3.1"

rules:
  - rule: unlikely_intent
    steps:
      - action: action_unlikely_intent
      - action: utter_unlikely
  - rule: default
    steps:
      - action: action_default_fallback
      - action: utter_default
